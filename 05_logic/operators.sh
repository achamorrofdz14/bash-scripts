#!/bin/bash

# Chaining commands with: &, &&, ||, ;
# Operators: -eq -ne -lt -gt -geq -leq
# String operators: = != -z(empty) -n(non empty)
# File operators: -e(exists)  -f(regular file) -d(directory) -x(executable file) -r(readeable file)

